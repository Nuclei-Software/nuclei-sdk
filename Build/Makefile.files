INCDIRS += $(NUCLEI_SDK_ROOT)/NMSIS/Include
C_INCDIRS +=
CXX_INCDIRS +=
ASM_INCDIRS +=

SRCDIRS +=
C_SRCDIRS +=
CXX_SRCDIRS +=
ASM_SRCDIRS +=

C_DEFINES +=
CXX_DEFINES +=
ASM_DEFINES +=
DEFINES +=

C_SRCS +=
CXX_SRCS +=
ASM_SRCS +=

LIBDIRS +=

include $(NUCLEI_SDK_BUILD)/Makefile.files.$(SOC)
